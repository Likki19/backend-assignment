spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ipl_dev
    username: dev_user
    password: dev_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 5051  # Development port
